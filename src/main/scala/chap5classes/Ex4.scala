package chap5classes

/**
 * 4. Reimplement the Time class from the preceding exercise so that the internal representation is the number
 * of minutes since midnight (between 0 and 24 × 60 – 1). Do not change the public interface. That is, client
 * code should be unaffected by your change.
 */
object Ex4 {
  // well, see Ex3, it's already implemented like suggested above.
}
