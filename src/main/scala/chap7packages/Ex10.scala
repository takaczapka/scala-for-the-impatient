package chap7packages

/**
 * 10. Apart from StringBuilder, what other members of java.lang does the scala package override?
 */
object Ex10 {

  // ANSWER
  // Let's see what's defined in java.lang vs what's in scala.Predef and scala._ (as those override java.lang)
  // - most of primitive wrapper classes (not Integer, as Scala uses Int)
  // - Iterable
}
